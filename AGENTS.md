# Digestube v2 — 작업 인계 문서

이 저장소에서 코드를 고치기 전에 이 문서를 읽는다.

---

## 최신 합의와 구현 — 2026-09-11 (아래 과거 기록보다 우선)

- 2026-09-12: 개발·평가·이력서는 `notes/20-development-evidence-framework.md`를 공통 기준으로 사용한다. 실패 정의 → 사전 통과 기준 → 후보 2~3개 → 자체 데이터 검증 → 결정·이력서 순서가 아래 과거 후보 전수 조사 절차보다 우선한다. 기존 10문항 검색은 합격선 없는 기준선 측정이며, 소급해 합격 판정하거나 모델 비교 선정으로 쓰지 않는다.

- 원본 자막 기반 시각 보정·재생 준비 전 클릭 처리·구두점 없는 한국어 종결 표현 분할을 적용했다. `notes/17-timestamp-release.md` 참조. 원본 응답은 비공개 Storage `transcript-sources`에 보관한다. 단어별 시각 정확도를 보장하지 않는다.
- 플레이어 로딩·재생 실패 시 영상 영역에 안내와 `유튜브에서 보기`·`다시 시도`를 제공한다. 직접 열기에는 현재 문단 시각을 유지한다. 자동 재생 차단은 별도 안내한다.
- 범위는 native 자막 등록 → 문단 분할 → 목차 → 읽기·의미 검색.
- 물어보기·핵심 하이라이트·최종 목차 모델 평가는 보류. Sonnet 5는 잠정 사용.
- 청킹은 전체 자막을 합쳐 문장 경계를 찾은 뒤 길이 기준으로 묶는다. 조각 내부의 마침표도 인식하고 자막 줄바꿈은 공백으로 처리한다. 엄밀한 재귀 분할은 아니다.
- 신규 전사 mode/요청 언어/응답 언어/완료 시각 기록. 과거 mode는 추정하지 않는다.
- 재등록은 확인 후 진행하며, 새 자막을 검증한 뒤 트랜잭션으로 교체한다.
- 목차 실패는 한 번 재시도, 이후 첫 문장을 말줄임표 포함 25자 이내로 대체한다.
- 목차는 요청당 4문단, 임베딩은 요청당 16문단 처리하고 `left`로 이어간다.
- 2026-09-10 운영 DB migration 적용 및 앱 커밋 `328200e` 배포 확인. 새 영상의 native 등록부터 목차·임베딩 완료까지 검증했다.
- 청킹 변경 시 신규 등록뿐 아니라 기존 라이브러리에도 적용해야 한다. `scripts/rechunk-library.ts`와 `notes/16-library-rechunk.md` 참고.
- 현재 구현·검증·운영 상태는 `notes/12-v2-finish.md`, 실행 방법은 `web/README.md` 참고.
- 아래 옛 작업 순서·Python 경로·후보 선정 설명은 역사적 기록이다. 현재 사실과 혼용하지 않는다.

## 1. 이 프로젝트가 뭔가

유튜브 영상을 전사해 **문단으로 나누고, 목차를 붙이고, 뜻으로 검색**할 수 있게 만드는 서비스.

v1(`../digestube`)을 **다시 만드는 것**이다. 기능을 옮겨오는 게 아니라, 기술 하나를 고를 때마다
**후보 나열 → 근거 → 측정** 순서를 다시 밟는다.

### 지켜야 할 원칙 (이게 이 프로젝트의 전부다)

1. **재기 전에 잣대를 정한다.** 재고 나서 기준을 정하면 원하는 쪽이 이기게 고를 수 있다.
2. **남의 벤치마크는 참고까지만.** 최종 판단은 우리 콘텐츠로 직접 잰 결과로 한다.
   (실제 사례: 참고하던 한국어 STT 벤치마크가 후보 회사가 직접 낸 표였다)
3. **지표 하나로 고르지 않는다.** CER 1등이 뜻 오류에서는 꼴찌였다. 지표를 하나 더 만들었다.
4. **숫자를 쓰면 그 숫자를 만든 스크립트를 같이 남긴다.** 지금 이게 제일 약한 부분이다.
5. **못 정한 것은 못 정했다고 적는다.** "임시로 쓰는 중"과 "재서 골랐다"를 구분해서 쓴다.

자세한 절차는 `notes/00-methodology.md`.

---

## 2. 지금 상태

**배포됨: https://digestube-v2.vercel.app**
**코퍼스: 영상 7편 · 문단 57개.** Supadata 무료 크레딧 100 을 다 썼다. 더 넣으려면 결제.

파이썬 구현은 `_old/web/` 로 치웠다. 지금 코드는 **Next.js 16 + TypeScript**(`web/`).

| 단계 | 상태 | 파일 |
|---|---|---|
| 오디오 추출 | 제약으로 결정 (직접 접근은 12/12 차단) | — |
| 전사 | 됨 · Supadata | `web/src/lib/supadata.ts` |
| 문단 나누기 | 됨 · recursive splitting (임시) | `web/src/lib/chunker.ts` |
| 임베딩 | 됨 · KURE-v1 (HuggingFace) | `web/src/lib/embed.ts` |
| 목차 생성 | 됨 · **Claude Sonnet 5** (후보 6개 재고 고름) | `web/src/lib/outline.ts` |
| 근거 대조 | 됨 | `web/src/lib/verify.ts` |
| 검색 | 됨 · pgvector `search_chunks` | `web/src/lib/search.ts` |
| 저장소 | **됨** · Supabase(서울) + pgvector | `web/src/lib/store.ts` |
| 답변 생성 | **안 됨** · 미착수 | — |
| 검색 임계치 | **잠정 0.55** · 확정 아님 | `notes/10-search-threshold.md` |

---

## 3. 코드 구조

```
web/                              Next.js 16 App Router
  src/app/
    page.tsx                      홈 — 영상·플레이리스트 넣기
    videos/page.tsx               라이브러리 (썸네일 그리드 · 삭제)
    videos/[vid]/page.tsx         영상 — 2단(왼쪽 영상·목차 / 오른쪽 전사문)
    search/page.tsx               검색 결과
    api/ingest/route.ts           전사 → 청킹 → 저장
    api/embed/route.ts            벡터 (벡터 없는 문단만)
    api/outline/route.ts          목차 + 근거 대조
    api/search/route.ts           검색
    api/playlist/route.ts         플레이리스트 → 영상 번호 목록
    api/videos/[vid]/route.ts     DELETE
  src/lib/
    supadata.ts    전사 · 플레이리스트
    chunker.ts     조각 → 문단
    embed.ts       문장 → 벡터 (KURE-v1, 1024차원)
    search.ts      검색 + 걸린 문장 짚기
    store.ts       저장소 — 키가 있으면 Supabase, 없으면 파일
    outline.ts     문단 → 목차 제목 + 근거 문장 (모델 상수 한 곳)
    verify.ts      근거 문장이 원문에 있는지 대조
    youtube.ts     제목·채널 (oEmbed)
  scripts/
    fill.mjs             코퍼스 채우기 (전사→벡터→목차)
    bench-outline.mjs    목차 모델 비교
    eval-search.mjs      검색 임계치 재기
    migrate.mjs          db.json → Supabase (한 번만)
  supabase/schema.sql    테이블 4개 + pgvector + search_chunks
_old/                    파이썬 구현·옛 화면. 참고용
notes/                   판단 기록. 결론만이 아니라 과정을 남기는 곳
```

### API

```
POST   /api/ingest              {url} → 전사·청킹·저장. 긴 영상은 {job} 반환
GET    /api/ingest?job=&vid=    전사 작업 조회
POST   /api/embed               {vid} → 벡터 없는 문단만. 반복해서 부르면 이어서
POST   /api/outline             {vid} → 목차 + 근거 대조
GET    /api/search?q=&vid=&k=
POST   /api/playlist            {url} → 아직 안 넣은 영상 번호
DELETE /api/videos/{vid}
```

화면 주소: `/` · `/videos` · `/videos/{vid}` · `/videos/{vid}#ck3` · `/search?q=`

**단계를 나눈 이유**: 서버리스는 한 요청이 60초를 못 넘는다. 전사·임베딩·목차를
한 번에 처리하면 긴 영상에서 통째로 실패한다. 이어 붙이는 일은 브라우저가 한다.

### 띄우는 법

```bash
cd web && npm run dev            # 화면 + API · 3000
```

`.env.local` 에 `SUPABASE_URL` · `SUPABASE_SERVICE_ROLE_KEY` · `SUPADATA_API_KEY` ·
`ANTHROPIC_API_KEY` · `HF_TOKEN` · `SUPADATA_MODE` 가 필요하다.

### 코퍼스 채우기는 로컬에서 한다

```bash
node --env-file=.env.local scripts/fill.mjs <vid> <vid> ...
```

배포(Vercel 무료)는 한 요청이 **60초**를 못 넘는데 `generate` 전사는 **91초** 걸린다.
로컬은 제한이 없고 저장소가 같은 Supabase 라 여기서 채운다. Supadata 무료 플랜은
동시 요청을 막으므로(429) 한 편씩 15초 간격으로 돈다.

### 키

- `~/.supadata-key` — Supadata
- `server/.env` 의 `ANTHROPIC_API_KEY` — 목차 생성
- HuggingFace 토큰 — `HF_TOKEN` 또는 `~/.cache/huggingface/token`

---

## 4. 지금까지 내린 결정과 근거

### 오디오 추출 · 전사 — Supadata (고른 게 아니라 남은 것)

로컬에서는 `yt-dlp` + `mlx-whisper`가 잘 돌았다. 그런데 **서버에서 유튜브가 막힌다.**

- 클라우드 2곳(Hugging Face Jobs, Vercel — 둘 다 AWS)에서 **12회 시도 12회 차단**
- 첫 요청에서 1초 안에 막힘. 누적 요청 때문이 아니다
- 러너 IP가 바뀌어도 막힘 → 개별 주소가 아니라 **대역 단위**
- 브라우저 증명서 방식도 6회 전부 실패
- 오디오 대신 자막만 가져와도 막힘

→ 서버가 오디오를 얻을 수 없으므로 로컬·클라우드 STT 후보가 **전부** 탈락. 대행 서비스만 남았다.

**`native`가 아니라 `generate`를 쓰는 이유**: 자막 출처(사람/기계)를 응답에서 구분할 수 없다.
사람 자막이면 자막이 낫고(12:4) 기계 자막이면 받아쓰기가 크게 낫다(16:4). 어느 쪽이 올지
모르므로 품질이 일정한 쪽을 택했다. 값은 더 든다(10분 영상 1크레딧 → 20크레딧).

**아직 안 잰 것**: Supadata 받아쓰기 품질을 검수된 정답지에 대본 적이 없다.

### 문단 나누기 — recursive splitting (임시)

같은 전사문(3편·8,186자)에 네 방식을 걸었다.

| | 방식 | 문장 중간 끊김 | 판정 |
|---|---|---|---|
| A | 글자 수로 | 88% | 탈락 |
| B | 마침표마다 | 0% (평균 58자) | 너무 잘아 뜻이 안 담김 |
| C | 문장 끝 + 길이 | 0% (평균 314자) | **지금 씀** |
| D | 화제 전환 | 미측정 | 임베딩 필요 |

겹침(overlap)은 붙여봤더니 끊김을 고치지 못하고(88%→90%) 데이터만 38~95% 늘어 **도입하지 않았다.**

**C는 재서 이긴 게 아니다.** 판정하려면 검색이 잘 되는지를 봐야 하고 골든셋이 필요하다.
지금 코퍼스로는 어느 방식이든 다 맞혀서 갈리지 않는다.

참고(외부 자료, 참고까지만): semantic chunking이 recursive를 항상 이기지 않는다는 결과가 있다
(NAACL 2025 Findings, "Is Semantic Chunking Worth the Computational Cost?"). 조각이 잘아져서
검색은 잘 되는데 답을 만들 맥락이 부족해지는 현상이 보고됐다.

### 임베딩 — KURE-v1

- 하는 일이 **한국어 검색**이라 한국어 검색 성능을 기준으로 골랐다
- 원래 BGE-M3를 DeepInfra로 부르려 했는데 **그쪽이 모델을 내렸다(410)**
- KURE-v1이 HuggingFace에서 서빙 중인 것을 확인하고 갈아탔다. **차원이 1024로 같아 저장 구조를 안 바꿨다**
- 넣을 때(문단)와 찾을 때(질문)가 **같은 모델**이어야 비교가 된다 → `embed.py` 한 곳에만 둔다

### 목차 생성 — Claude Haiku 4.5 (임시)

- 문단마다 1회 호출. `{label, quote}`를 받는다
- `quote`가 원문에 실제로 있는지 대조하고 **없으면 그 항목을 버린다** (`verify.py`)
- **모델을 고른 게 아니다.** 후보 15개 중 6개만 돌렸고 표본이 영상 3편·회당 2번이라 순위를 못 정한다
- **프롬프트를 다듬지 마라.** 프롬프트를 모델에 맞춰 손보면 나중에 후보를 나란히 놓고 잴 수 없다

### 저장소 — Supabase (결정만, 미구현)

- Postgres + pgvector. 문단 텍스트·시각·벡터를 **한 행**에 담아 조회가 한 번에 끝나게
- 벡터 전용 DB는 나머지 데이터용 DB가 또 필요해 저장소가 둘로 나뉜다 → 제외
- Supabase vs Neon을 끝까지 비교했고 **지역이 갈랐다**. Neon은 서울이 없다(싱가포르 왕복 70~90ms)
- 갈아탈 지점: 저장 500MB 초과 · 벡터 1,000만 개 초과 · 검색 10ms 이내 필요

---

## 5. 남은 일 (이 순서로)

### ① 코퍼스 늘리기 ← 지금도 1순위 (무료 크레딧 100 소진)

영상 1편으로는 아무것도 판정할 수 없다. Supadata Basic($5·300크레딧)이면 3~10분 영상 20편쯤 된다.
받아쓰기는 **1분에 2크레딧**.

영상은 축을 먼저 정하고 채운다 — 형식(강연·인터뷰·정보전달·잡담) × 길이(3분·10분·20분+) × 언어(한/영).
한 종류만 모으면 측정이 편향된다.

넣는 동안은 목차 자동 생성을 꺼두고, 나중에 `python3 web/scripts/outline.py --all`로 한 번에 돌리는 게 싸다.

### ② 골든셋 만들기

질문과 정답 구간의 쌍. 이게 있어야 문단 나누기·임베딩·임계치를 판정할 수 있다.

설계에서 갈리는 지점 둘:

- **정답을 문단이 아니라 시각 구간(초)으로 잡는다.** 문단으로 잡으면 방식마다 경계가 달라 비교가 안 된다
- **k를 문단 수가 아니라 글자 예산으로 맞춘다.** 상위 3문단이면 B는 174자, C는 942자를 보는 셈이라 공평하지 않다

질문 만들 때 **문단을 보여주면 안 된다.** 문단을 보고 질문을 만들면 그 문단의 어휘를 그대로 써서
글자 검색으로도 다 맞고, 방식이 갈리지 않는다. 전사문 전체를 읽고 질문을 쓴 뒤,
원문에 있는 낱말을 쓴 질문은 고쳐 쓴다.

유형을 섞는다: 사실 회수 / 설명 요구 / **답 없음**. 마지막은 검색 임계치를 정하는 데 반드시 필요하다.

**측정 시작 전에 골든셋을 동결한다.** 결과를 보고 고치면 원하는 방식이 이기게 만들 수 있다.

### ③ 저장소를 Supabase로

`store.py`의 함수 7개만 갈아끼우면 된다 — 부르는 쪽을 안 고치려고 인터페이스를 분리해뒀다.
`upsert_video` `get_video` `list_videos` `put_chunks` `get_chunks` `put_outline` `get_outline`

검색은 SQL 한 줄이 된다: `ORDER BY vector <=> '[질문벡터]' LIMIT k`

스키마:
```sql
create extension vector;
video    (id, title, channel, duration, lang, status)
chunk    (id, video_id, seq, t, t_end, text, vector(1024))
outline  (video_id, label, quote, t, seq)
bookmark (user_id, chunk_id, created_at)
```

### ④ 배포 (Vercel)

- `server.py` 라우트를 함수로 쪼갠다
- **`vercel.json`에 `"regions": ["icn1"]` 필수.** 안 적으면 워싱턴DC에서 돌아 서울 DB를 고른 의미가 사라진다
- 하루 한 번 예약 호출 — 무료 플랜 7일 멈춤 방지 + `pg_dump` 백업을 같이. **실패하면 알림까지**

### ⑤ 답변 생성

찾은 문단만 근거로 답을 만든다. 근거 밖의 내용은 만들지 않는다.
**유사도가 얼마 미만이면 답하지 않을지**를 정해야 하는데, 그 기준값은 ②의 "답 없음" 질문으로 정한다.

---

## 6. 알려진 결함 (고쳐도 좋음)

- `search.py` — 질의마다 문단 안 문장을 **전부 다시 임베딩**한다(하이라이트용). k=5면 최대 70문장,
  검색 한 번에 5~9초. 문장 벡터를 넣을 때 만들어 저장하면 사라진다
- `search.py` 주석의 "15,000개 0.34ms"는 **내적만 잰 값**이다. `store.get_chunks`가 질의마다
  `db.json` 전체를 파싱하는 비용은 빠져 있다
- `embed.py` — `HTTPError`만 잡는다. 타임아웃·`URLError`는 재시도 없이 터진다
- `embed.py` — 응답 모양을 검증하지 않는다. 차원이 다르면 `search.py`가 조용히 건너뛰어
  검색이 이유 없이 0건이 될 수 있다. `DIM` 체크를 여기서 하는 게 맞다
- `server.py` — `do_POST`에서 `get_video`를 두 번 부른다 (파일을 두 번 파싱)
- `server.py` — `::`에 바인드해 LAN에 포트가 열린다. 방어는 IP 체크 하나뿐

---

## 7. 하지 말 것

- **v1(`../digestube`)의 판단물을 가져오지 마라.** 특히 `data/golden_search.json`(정답지)과
  `scripts/evaluate.py`(평가 방식). 가져오면 v2의 판정이 v1의 판정에 묶인다.
  (프론트 표현 계층은 예외로 이미 참고했다 — `notes/07-frontend.md` 참고)
- **목차 프롬프트를 모델에 맞춰 다듬지 마라.** 후보 비교가 오염된다
- **재현 스크립트 없이 숫자를 남기지 마라.** 주석에 숫자만 있고 돌릴 코드가 없는 상태가 이미 여럿이다
- **"임시로 쓰는 중"을 "재서 골랐다"로 바꿔 적지 마라.** 지금 임시인 것: 문단 나누기 방식, 목차 모델

---

## 8. 더 읽을 것

- `notes/00-methodology.md` — 기술 선정 6단계
- `notes/02-stt.md` — 음성인식 후보 비교와 정답지 만들기
- `notes/04-chunking-embedding.md` — 임베딩 후보
- `notes/05-pending.md` — 아직 안 건드린 것
- `notes/07-frontend.md` — v1에서 무엇을 가져오고 무엇을 안 가져왔나
- `notes/08-outline.md` — 목차 생성 배관


---

## 9. 배포 환경에서 드러난 제약 (2026-09-08~09)

### Vercel 무료 플랜 60초

`generate` 전사는 작업 번호를 받는 데만 **91.8초**가 걸린다. 문서에 웹훅·콜백·
async 플래그가 없고 "AI 생성 요청은 최대 2분"이라 적혀 있어 나눠 부를 수도 없다.

- 배포는 `SUPADATA_MODE=native`(3초)로 돌린다 — 환경변수라 재배포 없이 바꾼다
- **코퍼스는 로컬에서 `generate` 로 채운다.** 저장소가 같은 Supabase 라 결과는 같다
- 아직 안 정한 것: 어디서 90초를 버틸 것인가.
  Vercel Pro($20/월·300초) · Supabase Edge Function · Supadata 유료(배치)
- Supadata 배치는 **유료 플랜 전용**(402). 무료로는 못 쓴다

### native 의 대가 — 언어를 못 정한다

`native` 는 그 영상에 붙은 자막을 가져오는데, **번역본이 여럿이면 아무거나 온다.**
한 영어 영상은 자막이 21개 언어로 있었고 `lang` 을 안 주니 **아랍어**가 왔다.
`lang=ko` 를 주면 영어 영상에 한국어 번역본이 온다.

→ 원어를 보장하는 것은 `generate` 뿐이다. 그것도 완벽하진 않다 — 한 한국어 영상이
`lang=ber` 로 영어 전사돼 나온 적이 있다(그 영상은 코퍼스에서 뺐다).

### Supadata 무료 플랜의 요청 한도

동시에 두 편을 걸면 429(`Request rate limit`)가 난다. 한 편씩 15초 간격으로 돈다.
크레딧을 다 쓰면 메시지가 `Plan usage limit`으로 바뀐다 — 둘은 다른 것이다.

## 10. 검색 하이라이트

문단 안에서 질문과 제일 가까운 문장을 짚어준다. 뜻으로 찾은 것이라 글자가 겹치지
않아 왜 걸렸는지 알 수 없기 때문이다.

**문장 최소 길이를 20자로 잡았다.** 그전에는 "근데 그게 싫더라고", "안 만나면 돼"
같은 것이 뽑혔다 — 그 자체로 뜻이 없고, 짧을수록 벡터가 불안정해 아무 질문에나
중간 점수가 나온다. 재서 정한 값이 아니라 결과를 보고 고른 값이다.

상위 3개만, 문단당 10문장까지 본다. 전부 하면 문장이 수십 개가 되어 느려진다.

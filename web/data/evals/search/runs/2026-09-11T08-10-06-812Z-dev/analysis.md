# 검색 기준선 해석

## 실행 조건

- 사용자 검수 완료 질문 10개: 답 있음 8개, 답 없음 2개
- 전체 라이브러리 8편·89문단, `top_k=3`
- 임베딩 `nlpai-lab/KURE-v1`, 검색 임계치 없음
- 운영 배포 `digestube-v2-5wz09wrvm@b6259f8`
- 실행 중 영상·문단·벡터 데이터 유지, 오류 0건

## 결과

| 항목 | 결과 | 해석 |
|---|---:|---|
| 시간 구간 자동 적중 | 8/8 | 상위 3개 중 정답 시간과 겹치는 문단이 있음 |
| AI 본문 확인 후 명확한 적중 | 7/8 | 기대 답을 직접 찾을 수 있음 |
| 부분 적중 | 1/8 | Q03은 핵심 문장이 두 문단으로 갈라져 앞부분만 검색됨 |
| 올바른 거절 | 0/2 | 임계치가 없어 두 질문 모두 결과 3개를 반환함 |
| 답 있는데 빈 결과 | 0/8 | 임계치가 없으므로 빈 결과 없음 |

Q03은 “과거 AI는 질문에 답만 한다”는 앞부분은 2위에 나왔지만, “사람의 개입 없이 계획하고 해결한다”는 뒷문단은 상위 3개에 없었다. 자막의 잘못된 마침표 때문에 하나의 설명이 두 문단으로 갈린 사례다. 시간 구간이 앞 문단과 겹쳐 자동 채점은 성공으로 계산했으므로, 자동 8/8만 성능 수치로 쓰지 않는다.

Q07은 영상 전체가 622자인 문단 하나라 그 영상이 검색되면 정답 구간도 자동으로 포함된다. 적중은 맞지만 세밀한 구간 검색 능력을 보여주는 문항은 아니다.

## 임계치 확인

답 있는 질문의 1위 점수는 0.545–0.667, 답 없는 질문은 0.544와 0.619였다. 두 분포가 겹친다. 특히 Q10의 무관 결과 점수 0.619가 여러 정상 질문보다 높다.

기록된 결과에 가능한 점수 경계를 모두 적용해 보니, 단순 임계치는 전체 정답 수를 현재 8/10보다 높이지 못했다. Q09를 거절할 만큼 높이면 정답 근거 점수가 낮은 Q07·Q08도 놓치기 시작한다. 따라서 이 10문항만 보고 임계치를 제품에 적용하지 않는다.

재현 명령:

```sh
node --import tsx scripts/analyze-search-threshold.ts data/evals/search/runs/2026-09-11T08-10-06-812Z-dev/results.json
```

## 응답 시간

- 첫 요청: 17.342초
- 이후 9개: 중앙값 1.765초, 1.474–2.967초

첫 요청에는 외부 임베딩 모델의 준비 시간이 포함됐을 가능성이 있지만 이 한 번의 실행으로 원인을 확정하지 않는다. 응답 시간은 검색 결과의 하이라이트 생성도 포함한다.

## 다음 판단

현재 우선 문제는 임계치 숫자가 아니라 다음 두 가지다.

1. 한 설명이 자막 오류로 이웃 문단에 갈린 경우, 검색 결과에서 이어지는 문단까지 읽을 수 있게 할지 검토한다.
2. 답이 없는 질문을 점수 하나로 구분하기 어려우므로 질문-문단 관련성을 한 번 더 판정하는 방법을 후보로 둔다.

개선안을 적용하기 전에 실패 정의와 비용·지연 기준을 먼저 정한다. 같은 10문항은 개발 비교에만 사용하고, 최종 확인에는 새 질문을 만든다.

## 한계

- 표본은 10문항뿐이며 전체 서비스 성능으로 일반화할 수 없다.
- 질문·정답은 사용자 검수를 받았지만, 검색 결과의 의미 적합성 분류는 AI가 본문을 읽고 한 1차 판정이다.
- 질문을 현재 코퍼스에서 만들었으므로 완전히 독립적인 평가셋은 아니다.
- 이 결과는 KURE-v1의 다른 임베딩 모델 대비 우위를 증명하지 않는다.
